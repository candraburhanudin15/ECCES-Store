import type { Hub, Integration } from '@sentry/types';
import type { VueOptions } from './types';
/**
 * Initialize Vue error & performance tracking.
 */
export declare class VueIntegration implements Integration {
    /**
     * @inheritDoc
     */
    static id: string;
    /**
     * @inheritDoc
     */
    name: string;
    private readonly _options;
    constructor(options?: Partial<VueOptions>);
    /** @inheritDoc */
    setupOnce(_addGlobaleventProcessor: unknown, getCurrentHub: () => Hub): void;
    /** Just here for easier testing */
    protected _setupIntegration(hub: Hub): void;
}
//# sourceMappingURL=integration.d.ts.map